# 深知 ShenZhi 部署环境变量示例
# 复制为 .env 后按需填写;不填这些变量也可正常运行(当前为纯前端 mock 数据)

# 生成 / 用户服务基址（含 /api/v1 的上一层）。前端请求同源 /api/v1，由 Next 转发。
# 示例:https://api.example.com 或 http://47.238.241.77:8080
# API_URL=
# NEXT_PUBLIC_API_URL=

# NextAuth 认证(规划中,接入时再填)
# AUTH_SECRET=

# 数据库连接(规划中,接入时再填)
# DATABASE_URL=