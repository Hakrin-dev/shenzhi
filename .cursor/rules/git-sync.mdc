---
description: 提交后同步 fork 与主仓库 PR，禁止只留本地 commit
alwaysApply: true
---

# Git 提交与远程同步

## Remote 约定

| Remote | 仓库 | 用途 |
|--------|------|------|
| `upstream` | `Hakrin-dev/shenzhi` | 主项目（对照基线） |
| `origin` | `zixuanzheng2007-stack/shenzhi` | fork（推送与开 PR） |

日常开发分支：`dev`，跟踪 `upstream/dev`。

## 每次提交必须完成的步骤

用户要求提交，或任务结束产生 commit 时，**不得只 commit 不 push**：

1. `git fetch upstream`
2. 若本地 `dev` 落后于 `upstream/dev`：先 `git rebase upstream/dev`（有冲突则解决后再继续）
3. 创建 commit（仅包含用户要求的改动；不提交 `.env`、密钥、`_local-only/`）
4. **立即** `git push origin dev`（同步 fork 历史）
5. 若需合入主仓库：确认 PR 指向 `Hakrin-dev/shenzhi` 的 `dev`；无 PR 则 `gh pr create --repo Hakrin-dev/shenzhi --head zixuanzheng2007-stack:dev --base dev`

## 禁止

- 只本地 commit、不同步 `origin`
- 基于 fork 旧历史（含 B 栈）rebase，而不先对齐 `upstream/dev`
- 未经用户明确要求向 `upstream` 直接 push（主仓库通常通过 PR 合并）

## 提交后汇报

向用户说明：commit hash、是否已 push 到 fork、PR 链接（若已创建或更新）。
